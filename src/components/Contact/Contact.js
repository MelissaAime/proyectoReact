import { Form } from "../Form/Form"

export const Contact = () => {

    return(
        <div className="container my-3">
            <h2>Contactanos...</h2>
            <Form/>
        </div>
    )
}